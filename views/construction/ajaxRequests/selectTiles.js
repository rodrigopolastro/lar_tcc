roomTileInputDiv.addEventListener('click', selectTiles);

function selectTiles(){
  alert("listando pisos disponíveis");
  // Here goes the AJAX request to select all the available tiles and
  // display them in the bootstrap popover.

  //OBS:
  //When a tile image is clicked, the variables 'tileId' and 'tileName' are set.
}

function listTiles(){
}